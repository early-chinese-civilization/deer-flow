=================================================================
  DeerFlow has been deployed successfully!
=================================================================

Release Name: {{ .Release.Name }}
Namespace:    {{ .Release.Namespace }}

-----------------------------------------------------------------
  Access the application:
-----------------------------------------------------------------
{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
  URL: http://{{ .host }}
{{- end }}
{{- else }}
  Export the port:
    export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "deer-flow.name" . }},app.kubernetes.io/component=nginx" -o jsonpath="{.items[0].metadata.name}")
    kubectl port-forward $POD_NAME 8080:{{ .Values.nginx.externalPort }}
    Then visit: http://localhost:8080
{{- end }}

-----------------------------------------------------------------
  Services:
-----------------------------------------------------------------
  Gateway:     http://{{ .Release.Name }}-gateway:8001
  LangGraph:   http://{{ .Release.Name }}-langgraph:2024
  Frontend:    http://{{ .Release.Name }}-frontend:3000
{{- if .Values.nginx.enabled }}
  Nginx:       http://{{ .Release.Name }}-nginx:{{ .Values.nginx.externalPort }}
{{- end }}
{{- if .Values.provisioner.enabled }}
  Provisioner: http://{{ .Release.Name }}-provisioner:8002
{{- end }}

-----------------------------------------------------------------
  Check deployment status:
-----------------------------------------------------------------
  kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/instance={{ .Release.Name }}"
  kubectl get svc -n {{ .Release.Namespace }} -l "app.kubernetes.io/instance={{ .Release.Name }}"

=================================================================
